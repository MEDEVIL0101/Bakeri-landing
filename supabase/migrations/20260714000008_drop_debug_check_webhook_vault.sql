-- Cleanup for 20260714000007_debug_check_webhook_vault.sql — nothing to
-- drop (it never created any object, just RAISE NOTICE'd), kept as a
-- no-op migration for a clean paper trail matching this repo's existing
-- debug_*/drop_debug_* migration pairs.
SELECT 1;
